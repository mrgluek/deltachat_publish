version: '3.8'

services:
  deltachat_publish:
    build: .
    container_name: deltachat_publish
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
